name: meshcore-sim

runtime: docker

dependencies:
  - bun
  - typescript
  - git
  - gh
  - node@24
  - claude-code

grants:
  - github
  - ssh:github.com
  - claude
  - npm

volumes:
  - name: node-modules
    target: /workspace/node_modules

