node_modules/
dist/
docs/.api-tmp/
*.log
*.tsbuildinfo
.DS_Store

# bun is the canonical package manager; its lockfile (bun.lockb) is committed.
# npm's lockfile, if a fallback `npm install` ever writes one, is ignored.
package-lock.json
